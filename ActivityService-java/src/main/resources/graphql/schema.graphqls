#directive @shareable repeatable on OBJECT | FIELD_DEFINITION
#
#type Query {
#  activities: [Activity]
#}
#
#type Activity @shareable {
#  id: ID!
#  name: String
#}
directive @lookup(by: String) on FIELD_DEFINITION

type Product
{
  id: ID!
  name: String
}

type Query
{
  products: [Product]
  productById(id: ID!): Product @lookup
}